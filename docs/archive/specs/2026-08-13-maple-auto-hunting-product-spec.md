# 冒险岛怀旧服自动打怪工具：完整产品规格

日期：2026-08-13

状态：已确认的产品规格交接稿；后续实现方须按本文门禁补齐实现细节

目标客户端：窗口标题为“冒险岛怀旧服”的 Windows 客户端

目标平台：Windows 10/11 x64

适用前提：用户拥有明确授权的测试环境

> 本文是新增的上层产品规格。既有的 `docs/2026-08-13-input-compatibility-handoff-spec.md` 保持不变，作为输入兼容性探测交接记录；既有的 `docs/superpowers/specs/2026-08-12-maple-visual-automation-design.md` 是历史架构草案。本文对冲突项作出最新决策。

## 1. 产品定义

本产品是一个独立的 Windows 桌面脚本客户端，用屏幕视觉理解游戏状态，并自动完成打怪循环：识别自己的角色、其他玩家和怪物；在地图平台之间移动；选择单体或群攻；按 HP/MP 阈值使用药水；在战斗空档拾取掉落；遇到无法确认的状态时暂停并提供可复盘信息。

首个可交付版本要求功能面完整，不将产品拆成面向用户的“简化版”和“完整版”。实现上仍必须按模块和阶段门禁推进，先通过输入设备与离线视觉验收，再逐项开启真实动作。任何未通过门禁的能力都不能静默进入运行闭环。

### 1.1 明确范围

包含：

- 单个指定窗口化客户端；
- 常驻脚本客户端 UI 与客户区实时镜像预览；
- 人物（自己/其他玩家）和怪物识别及颜色框标记；
- 固定 UI 的图色/OpenCV 与 OCR 识别；
- YOLO/ONNX 动态目标检测；
- 地图档案、平台/梯子拓扑和跨层导航；
- 单体优先、自动、群攻优先三档攻击模式；
- HP/MP 百分比或最低数值补给；
- 自动拾取，默认 `Z`，支持自定义；
- 卡死、死亡、断线、未知弹窗、切图和输入设备异常的安全停机；
- Windows 自包含发布包、会话日志、截图/叠加回放和诊断报告。

不包含：

- 读取或修改游戏内存；
- 注入客户端、修改网络协议或向后台窗口发送消息；
- 多账号/多窗口并行；
- 未验证地图、未知传送装置、移动平台或未知 UI 上的自动运行；
- 未经用户确认的云端图像上传；
- 任何以隐藏自动化、规避检测或规避封禁为目标的功能。

### 1.2 最新决策记录

| 主题 | 最新决策 |
| --- | --- |
| 输入 | 现有测试证明标准 `SendInput` 调用成功但客户端无响应；`SendInput` 只保留为诊断探针，不是生产路径。生产路径必须通过独立输入适配器，首选虚拟 HID；其他驱动方案只有在独立兼容性、签名和卸载测试通过后才能作为候选。 |
| 内存 | 不使用只读内存辅助；视觉结果和安全状态是唯一运行依据。 |
| 推理 | OpenCV/图色处理固定 UI 和几何校验；YOLO/ONNX 处理动态人物与怪物；CPU 是基线，GPU provider 可选。 |
| 云端 | 只支持内置的百炼配置；用户选择内置模型并填写 API Key 即可。百炼只在建图、低置信度复核或用户主动重校准等低频场景调用；不在每帧循环中调用，未配置时增强功能保持禁用。 |
| 预览 | 常驻独立脚本客户端窗口，镜像游戏客户区；游戏原窗口不加覆盖层、不修改画面、不因预览抢焦点。 |
| 标记 | 自己角色绿色、其他玩家青色、怪物红色；掉落物不在预览中检测或标记，但拾取通道可进行不显示的内部观察。 |
| 玩家 | 其他玩家不参与目标选择，出现时仍正常打怪。 |
| 拾取 | 默认 `Z`，可自定义；拾取是完整产品能力。 |
| 默认键位 | 移动 `Left/Right/Up/Down`，跳跃 `Alt`，拾取 `Z`；技能和药水由用户配置。 |

## 2. 用户工作流与 UI

### 2.1 主窗口

主窗口是常驻运行台，不是只在调试时显示的工具。布局采用“左侧控制栏 + 右侧客户区镜像预览 + 底部状态栏”：

- 左侧：目标窗口身份、运行状态、启动/暂停/紧急停止、HP/MP、攻击模式、功能开关、补给阈值和当前地图档案；
- 右侧：客户区镜像及动态识别框；
- 底部：采集 FPS、识别 FPS、帧延迟、当前状态、角色所在层、目标数量、输入适配器状态和最近暂停原因；
- 二级页面：地图档案、动作配置、模型/阈值、百炼配置、日志回放、设备诊断和版本信息。

UI 要求高对比度、可读、可缩放；不在游戏窗口内创建透明覆盖层。预览窗口与主窗口可以合并为一个程序窗口，但镜像区域必须保持独立渲染线程和原始宽高比。

### 2.2 客户区镜像预览

预览只捕获游戏客户区，不包含标题栏和边框。默认采用最新帧优先的有界队列：

```text
游戏客户区 -> 采集线程 -> 带 frameId 的帧缓冲
                         -> 识别线程
                         -> 叠加渲染线程 -> 主窗口预览
```

预览永久显示，但预览不可用时不得继续输入。窗口最小化、遮挡导致黑帧、客户区尺寸改变、采集后端失败或帧超过 TTL 时，镜像显示明确的“画面不可用/已暂停”状态。

只标记动态对象：

- 自己角色：绿色框，标签包含 `Self`、置信度和跟踪编号；
- 其他玩家：青色框，标签包含 `Player`、置信度和跟踪编号；
- 怪物：红色框，标签包含类别、置信度和目标编号；
- 掉落物：不在预览检测或标记，不画框、不显示标签；拾取所需观察仅通过内部通道提供；
- HP/MP 条、数字、地图名、技能栏等静态 UI：仅内部识别，不画框。

自己角色识别采用“自动识别 + 可纠正”：先通过 YOLO、头顶血条/角色特征、初始位置和多帧轨迹推断；置信度不足时在预览中提示用户点击一次自己的角色。确认后使用时序跟踪，客户端重启、PID/HWND 改变或地图身份重置时要求重新确认。

其他玩家只做观察标记，不暂停、不攻击、不作为导航目标。怪物框只代表观察结果，不直接触发按键。

### 2.3 首次配置流程

1. 发现并绑定目标窗口。
2. 校验客户端进程身份、客户区尺寸、DPI 和采集后端；固定 HUD 的 ROI 由预设锚点和自动标定计算，不要求用户逐项框选。
3. 创建/选择地图档案；未知地图只能进入建档流程，不能直接运行。
4. 导入截图或短录屏帧，可选请求百炼生成地图结构候选。
5. 用户确认候选平台、梯子/绳索、边界和跨层连接；本地拓扑校验及动作预览通过后保存已验证版本。
6. 配置攻击、药水、拾取键和阈值，执行键冲突检查。
7. 执行输入设备单键诊断和人物/怪物离线回放检查。

### 2.4 启动与手动接管

启动必须经过显式 arm、短倒计时和目标窗口前台确认。用户手动键盘/鼠标操作、全局停止热键、窗口失焦、系统锁屏或睡眠都会使会话暂停或紧急停止。恢复不能沿用旧动作队列，必须重新采集稳定帧并由用户确认恢复；未完成会话不自动续跑。

## 3. 总体架构与接口

```text
Window Identity + Capture
          |
          v
ObservationSnapshot (frameId, monotonic time, coordinate space)
      |                 |
      v                 v
Fixed UI Pipeline       Dynamic Object Pipeline
OpenCV / OCR            YOLO / ONNX + tracker
      \                 /
       v               v
       Perception Fusion + Confidence / Freshness Gate
                         |
                         v
              Map Topology + FSM + Action Policy
                         |
                         v
                 Input Adapter Gateway
                         |
                         v
              HID device (implementation-specific)
```

模块只能通过结构化契约通信：

- `CaptureFrame`: 客户区图像、`frameId`、单调时间、窗口/DPI 信息；
- `ObservationSnapshot`: 角色、人物、怪物、内部掉落观察、平台、梯子、HP/MP、地图身份、坐标系、置信度、模型/ROI 版本和 stale TTL；
- `AbstractAction`: `MoveLeft`、`MoveRight`、`ClimbUp`、`ClimbDown`、`Jump`、`SingleAttack`、`AreaAttack`、`UseHpPotion`、`UseMpPotion`、`Pickup`；
- `InputResult`: 设备状态、发送时间、key-down/key-up 配对、失败原因和后置观察结果。

YOLO、云端模型和输入设备都不能直接决定按键；只有状态机通过安全门后，输入网关才可发送抽象动作对应的键盘报告。

## 4. 窗口身份、采集和坐标系

### 4.1 窗口绑定

标题“冒险岛怀旧服”只是初筛。正式绑定至少保存：`HWND`、PID、进程启动时间、规范化可执行文件路径、文件版本/哈希、客户区矩形和 DPI。每次发送前重新验证；HWND 重建、PID 复用、客户端重启或路径/版本变化都必须解除绑定并重新 arm。

HID 是系统级设备，不能真正把报告限制在 HWND；前台/进程检查只是用户态安全闸门。检查与发送之间出现竞态时，以停止和中性报告为准。

### 4.2 坐标系

所有模块明确使用以下坐标系：

- `ClientPixels`：当前客户区像素；
- `Viewport`：归一化后的当前可见区域；
- `MapWorld`：地图档案中的平台/梯子拓扑坐标。

镜头滚动或屏幕平移时，通过稳定背景/平台和角色轨迹估计 camera offset，将 `ClientPixels` 投影到 `MapWorld`。镜头未稳定、推断冲突或对象在屏外时，禁止导航动作，先等待或重规划。

### 4.3 帧新鲜度和采集后端

采集使用 PerMonitorV2 DPI awareness，记录客户区而非窗口外框；处理多显示器、窗口移动、缩放、遮挡、最小化、HDR/黑帧和采集后端差异。帧队列有界、latest-frame-wins；每类观察定义 TTL，过期观察不得驱动输入。采集、识别和渲染异步运行，YOLO 延迟不能阻塞 HP/MP 安全监控。

## 5. 视觉识别模块

### 5.1 OpenCV 图色与 OCR

OpenCV 负责窗口锚点、HP/MP 条、固定 UI、颜色/模板/边缘/轮廓和平台几何二次校验。HSV 容差、模板版本和多分辨率标定必须纳入模型 manifest；亮度、特效、DPI 或 UI 版本变化导致置信度下降时暂停。

HP/MP 支持两种互斥阈值模式：

- `percent`：使用血条比例；
- `absolute`：使用 OCR 当前值与最大值。

两通道冲突、OCR 过期、最大值未知或读数格式不合法时进入保护状态。喝药后必须在复核窗口检查读数变化或冷却状态；库存未知、连续触发超限或药水无反馈时暂停。

### 5.2 YOLO 与时序跟踪

预览叠加的动态类别为 `self`、`player`、`monster`。拾取所需的 `loot` 只作为内部观察通道，可由轻量图色/模板或独立检测器提供；它绝不绘制框、标签或数量，也不进入预览画面。YOLO 输出只包含类别、边界框、置信度、`frameId` 和模型版本。通过时序跟踪、去重、滞回、遮挡处理和 stale TTL 形成稳定目标；目标数量使用去重后的当前帧观察，不使用过期框。

模型 manifest 必须记录类别、输入尺寸、阈值/NMS、训练数据版本、权重哈希、推理后端和许可证。ONNX Runtime CPU 是基线；CUDA/TensorRT 仅为可选 provider，缺失时降级而不改变安全阈值。模型完整性或版本不匹配时禁止运行。

## 6. 地图建档与跨层导航

### 6.1 候选与真值分离

单张截图或云端输出只能生成候选结构，不能证明完整碰撞和可达性。地图档案分为 `candidate`、`validated`、`archived` 三态。候选必须经过用户确认、本地拓扑约束检查和动作预览/短时实地验证，成功后才能成为 `validated`。

### 6.2 拓扑对象

每个平台保存 MapWorld 水平范围、层 ID、边界和可站立条件；每个梯子/绳索保存起止平台、挂接/脱离条件和方向；边定义为 `walk`、`jump`、`climb` 或 `drop`，并包含安全距离和验证状态。未知传送门、移动平台、弹簧或不可解释地形默认阻止运行。

### 6.3 动作-观测闭环

导航不是一次生成整条按键脚本。每次只规划短动作窗口：检查前置条件 -> 发送动作 -> 观察角色位移/楼层/镜头变化 -> 判断后置条件 -> 成功则重规划，失败按有限预算重试后暂停。梯子挂接、跳跃落点、边缘下落和镜头滚动都必须有视觉反馈。

### 6.4 地图数据来源

地图档案可以从两类来源建立，优先使用无需录制的离线资料：

- **授权的离线客户端资产/地图元数据导入**：实现方可在用户有权使用的测试客户端中解析静态地图资源，生成平台、梯子和边的候选拓扑。导入结果必须记录客户端版本、资源文件哈希和解析器版本；资源解析只用于离线建档，不得在运行时读写游戏内存、注入客户端或修改资源。
- **本地截图/短录屏辅助建档**：当没有可用的离线资料时，从客户区帧生成候选结构，再由用户确认并进行短时实地验证。

无论来源如何，静态资料都不能替代运行时的角色、碰撞和镜头反馈；未验证候选不得进入自动动作闭环。地图版本或资源哈希变化时，档案自动降级为 `candidate` 并要求重新验证。

## 7. 自动打怪状态机

状态优先级从高到低：

```text
EmergencyStop
> InputUnavailable / FocusLost
> Death / Login / Disconnected / UnknownDialog
> MapTransition / Recalibrating
> CriticalPotion
> Loot
> Navigation
> Attack
> Idle
```

高优先级状态可以抢占低优先级状态；抢占时取消动作队列、释放动作键并记录原因。恢复必须有稳定帧、窗口/设备/地图/角色/HP/MP 五项健康检查，必要时要求用户确认。

### 7.1 战斗

- 目标选择只使用 `monster`，按置信度、距离、当前平台和地图拓扑过滤；`player` 永不进入目标列表。
- `single-first`、`auto`、`area-first` 三档模式支持目标数量阈值滞回和最短切换间隔，避免边界抖动。
- 技能未配置、冷却中、攻击无反馈或目标丢失时使用有限回退策略，超过预算暂停。
- 攻击动作由目标几何关系、动画/命中反馈、技能冷却和角色位移驱动；计时器只作为上限，不是唯一依据。

### 7.2 补给

默认优先级为 `HP > MP`。percent/absolute 模式必须显式选择；HP 与 MP 同时低时先处理 HP。药水按键有最小间隔、每分钟上限、动作后复核和无反馈熔断。HP critical 且无法确认补给成功时进入 EmergencyStop。

### 7.3 拾取

拾取配置至少包含：`enabled`、`key`（默认 `Z`）、触发策略、最大保持时间、重试次数、无掉落超时和背包满策略。只在怪物威胁低、角色与掉落处于同层/安全范围且 loot 状态由内部视觉确认时拾取；掉落物不在预览中标记。背包满、掉落不可达、拾取无反馈或角色死亡时退出拾取并暂停/转入更高优先级状态。

### 7.4 卡死与生命循环

卡死判断结合角色位移、场景变化和动作反馈，不能只依赖固定时间。恢复动作有有限次数和安全范围，超过预算暂停。死亡、复活界面、登录界面、断线、维护画面、未知弹窗、背包满和客户端崩溃默认不自动点击、不自动复活、不自动换频道，进入人工处理状态。

## 8. 动作节奏与自然调度

动作调度采用事件驱动和反馈闭环，不使用固定周期“每 N 毫秒按一次”的主循环。移动、攻击、跳跃、喝药和拾取的时机由目标几何关系、角色位移、动画阶段、技能冷却、画面稳定性和上一个动作的后置条件决定。

每个动作定义最短/最大保持时间、结束条件、同类动作最小间隔、超时和重试预算。允许使用有上下限、可关闭、可记录、可复现的受约束时间变化，以适应动画帧边界和客户端响应抖动；不得把随机性用于隐藏自动化或规避检测。任何无反馈动作都应重新观察或暂停，而不是无限重复。

## 9. 百炼视觉增强

### 9.1 允许场景

首次建图、用户主动重建、分辨率/UI 版本变化后的辅助校准、连续低置信度复核。运行中调用前必须暂停输入，并明确显示上传范围和预计调用成本。

### 9.2 数据与响应

API Key 只保存于 Windows Credential Manager/DPAPI；上传截图/录屏前需用户确认，说明图片范围、脱敏、留存和删除策略。请求设置尺寸上限、超时、重试上限、频率和成本预算；响应必须通过 JSON schema、拓扑/物理约束、本地回放和用户确认。模型不得返回或直接执行按键序列。

### 9.3 简化配置

首个交付只提供百炼一个云端厂商，不做通用供应商网关。设置页只需要：启用增强开关、内置模型下拉框、API Key 输入和“测试连接”按钮。API Key 缺失、连接测试失败或模型不在内置清单时，增强能力显示为不可用，但本地 OpenCV/YOLO 功能仍可独立运行。模型清单由应用版本固定并通过发布 manifest 校验，后续增加模型需随版本更新，不接受任意 URL 或任意远程插件。

## 10. 配置、日志与回放

配置采用带 schemaVersion 的 JSON，支持迁移、默认值、原子写入、文件锁、备份、损坏回退和冲突校验。逻辑键使用物理键捕获器转换，保存键盘布局/映射版本；移动、跳跃、攻击、药水和拾取键冲突时阻止保存或要求明确确认。

运行数据写入 `%LOCALAPPDATA%\\Maple...`，不写 EXE 安装目录。每个 session 保存结构化 JSONL 事件、session manifest、截图/叠加帧索引和版本信息；日志写入失败、磁盘满、截图配额超限都必须进入暂停或降级状态，不静默吞错。API Key 不得进入日志、崩溃转储或导出报告。

回放模式默认禁止真实输入，支持原始客户区帧、叠加标记帧、时间轴、ObservationSnapshot、抽象动作和暂停原因同步查看。

## 11. 输入适配器与发布包

### 11.1 适配器契约

输入适配器提供 `KeyDown`、`KeyUp`、`Press`、`ReleaseAll`、`Heartbeat` 和能力查询。设备状态与客户端响应分三层记录：设备安装/连接成功、操作系统收到键盘报告、客户端画面观察到响应。不能把 API 返回成功当成客户端响应成功。

HID 为系统级设备，不能提供真正的 HWND 定向隔离。用户态每次发送前检查目标进程身份和前台状态；检查与发送发生竞态、设备断连、心跳超时、进程崩溃、系统睡眠/锁屏或显示器/RDP 断开时，发送中性报告并将会话置为 EmergencyStop。未完成会话不可自动续跑。

### 11.2 发布物

发布物分为：

- `Runtime Bundle`：win-x64 self-contained 单文件 UI/策略程序，普通权限运行；模型/native provider 用 manifest hash 校验，数据写入用户目录；
- `Driver/Adapter Bundle`：如输入适配器需要系统组件，独立处理签名、Secure Boot、管理员权限、重启、安装、卸载、回滚、失败清理和 Defender/SmartScreen 兼容性。

运行程序不得静默安装或加载系统级组件；启动前检查适配器能力、版本、心跳和中性报告能力。CPU ONNX provider 是无 GPU 基线，GPU provider 缺失时可降级；模型、原生库、第三方许可证和权重来源必须列入发布 manifest。

## 12. 异常、测试与验收

### 12.1 必须进入暂停/紧急停止的条件

失焦、HWND/PID/路径变化、黑帧/过期帧、角色丢失、目标 stale、地图不匹配、镜头未稳定、HP/MP 冲突、药水无反馈、输入设备断连、未知弹窗、死亡/登录/断线/维护画面、日志写失败、磁盘满、系统睡眠/锁屏、客户端崩溃。

### 12.2 测试矩阵

覆盖 Windows 10/11、100/125/150/200% DPI、多分辨率窗口、单/多显示器、CPU/GPU、遮挡/最小化/HDR、客户端重启、镜头滚动、切图/换频道、怪物重叠、技能特效、HP/MP 变化、掉落密集、异常弹窗、设备断连和长时间 soak。

### 12.3 量化验收

- 人物/怪物检测按地图、分辨率、特效和遮挡分层统计 precision/recall、位置误差和误触发率；
- HUD/OCR 正确率、地图身份准确率、角色跟踪丢失率和 stale 率达到项目设定门槛；
- 采集/识别 p95 延迟、内存增长、丢帧、动作响应延迟和日志完整性在 4/8 小时 soak 中达标；
- 端到端误动作次数为零，暂停响应在设定上限内，stuck-key 为零；
- 输入验收必须分别记录设备安装、OS 报告和客户端响应三层结果；
- 未验证地图、未知弹窗、未知地形和不匹配版本必须阻止启动。

## 13. 实现状态与未决项

当前仓库只有 Windows Forms/PowerShell 输入探针，没有 `.csproj/.sln`、视觉运行时代码或 ONNX 模型；`Program.cs` 中的 `MapleVhfKeyboard` 仅是探测占位，不代表 HID 驱动可用。现有探针的按钮和脚本只覆盖固定的少量测试键，不等于规格要求的任意逻辑键、长按、重复、间隔和客户端响应报告能力。后续实现方仍需确定：

- 首批地图、怪物类别、训练数据和精度门槛；
- 采集后端基准（Windows Graphics Capture/BitBlt/DXGI）；
- 百炼预设模型、上传留存和成本预算；
- 输入适配器具体实现、签名与安装矩阵；
- OpenCV、YOLO、ONNX Runtime、OCR、模型权重和百炼 SDK 的许可证清单。
